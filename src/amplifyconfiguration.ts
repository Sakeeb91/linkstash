/**
 * Amplify Configuration
 *
 * IMPORTANT: This file is a placeholder. After running `npm run sandbox`,
 * copy the contents of `amplify_outputs.json` (generated at project root)
 * into this file's default export.
 *
 * Example after sandbox generates outputs:
 *
 * const amplifyConfig = {
 *   auth: {
 *     user_pool_id: "us-east-1_xxxxx",
 *     user_pool_client_id: "xxxxx",
 *     ...
 *   },
 *   data: {
 *     url: "https://xxxxx.appsync-api.us-east-1.amazonaws.com/graphql",
 *     ...
 *   }
 * };
 *
 * @see https://docs.amplify.aws/gen2/start/quickstart/
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
const amplifyConfig: Record<string, any> = {
  // Placeholder - replace with amplify_outputs.json contents after running sandbox
  // Run: npm run sandbox
  // Then copy amplify_outputs.json contents here
};

export default amplifyConfig;
