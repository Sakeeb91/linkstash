/**
 * Collections Components Barrel File
 *
 * Re-exports all collection-related components for convenient imports.
 * These components handle collection display, creation, and management.
 *
 * @example
 * import { CollectionCard, CollectionList, CollectionSelector } from '@/components/collections';
 */

// Components will be exported here as they are created
export {};
